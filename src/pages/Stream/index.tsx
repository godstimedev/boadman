import { DashboardHeader } from '@/components';
import { Container } from './styles';

const Stream = () => {
	return (
		<Container>
			<DashboardHeader />
			<h1>Stream</h1>
		</Container>
	);
};

export default Stream;
