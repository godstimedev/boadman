import { Container } from './styles';
import { DashboardHeader } from '@/components';

const Settings = () => {
	return (
		<Container>
			<DashboardHeader />
			<h1>Settings</h1>
		</Container>
	);
};

export default Settings;
