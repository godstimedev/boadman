import { DashboardHeader } from '@/components';
import { Container } from './styles';

const Teams = () => {
	return (
		<Container>
			<DashboardHeader />
			<h1>Teams</h1>
		</Container>
	);
};

export default Teams;
