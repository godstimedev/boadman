import { DashboardHeader } from '@/components';
import { Container } from './styles';

const Leaderboard = () => {
	return (
		<Container>
			<DashboardHeader />
			<h1>Leaderboard</h1>
		</Container>
	);
};

export default Leaderboard;
