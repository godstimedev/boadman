import { DashboardHeader } from '@/components';
import { Container } from './styles';

const Tournaments = () => {
	return (
		<Container>
			<DashboardHeader />
			<h1>Tournaments</h1>
		</Container>
	);
};

export default Tournaments;
