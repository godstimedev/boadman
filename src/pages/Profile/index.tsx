import { DashboardHeader } from '@/components';
import { Container } from './styles';

const Profile = () => {
	return (
		<Container>
			<DashboardHeader />
			<h1>Profile</h1>
		</Container>
	);
};

export default Profile;
