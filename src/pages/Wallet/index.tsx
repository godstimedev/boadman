import { Container } from './syles';
import { DashboardHeader } from '@/components';

const Wallet = () => {
	return (
		<Container>
			<DashboardHeader />

			<h1>Wallet</h1>
		</Container>
	);
};

export default Wallet;
